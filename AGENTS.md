# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# GymMate — règles agent (toujours)

Avant chaque génération de code, respecter les règles Cursor dans `.cursor/rules/` :

1. `gymmate-jalons.mdc` — progression J1→J10, pas d’anticipation, suivi dans `docs/suivi.md`
2. `gymmate-architecture.mdc` — structure `src/`, services vs écrans, Expo SDK 57
3. `gymmate-metier-firebase.mdc` — acceptation = relation/séance/chat ; modèle Firestore
4. `solid.mdc` — SRP, OCP, LSP, ISP, DIP (écrans ≠ services, types étroits)

Source pédagogique : `GymMate_Projet_Fil_Rouge_React_Native_Firebase.pdf`.

**Rappel** : UI locale jusqu’à J3 ; Firebase seulement à partir de J4.

**Domaine** : CrossFit uniquement (pas multi-sports).
